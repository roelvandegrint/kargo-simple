apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:  
  name: guestbook-simple
spec:
  ingressClassName: traefik
  rules:
    - host: guestbook-simple-dev.dev.vpp.eneco.com
      http:
        paths:
          - backend:
              service:
                name: guestbook-simple
                port:
                  name: http
            path: /
            pathType: Prefix
